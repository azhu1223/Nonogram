#include "Game.h"

#include <iostream>

int main() {
    bool keepPlaying = true;
    while (keepPlaying) {
        Game game;

        keepPlaying = false;
    }

    std::cout << "Exiting program.\n";

    return 0;
}